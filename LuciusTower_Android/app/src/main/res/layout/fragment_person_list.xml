<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context=".person.PersonListFragment">

    <include
        android:id="@+id/tool_bar"
        layout="@layout/include_common_tool_bar"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_person_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constrainedHeight="true"
        app:layout_constraintTop_toBottomOf="@+id/tool_bar"
        app:layout_constraintBottom_toTopOf="@+id/bt_debug_add" />

    <TextView
        android:id="@+id/bt_debug_add"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="15dp"
        android:gravity="center"
        android:background="#CCCCCC"
        android:textColor="#212121"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="测试添加一个DB数据" />


</androidx.constraintlayout.widget.ConstraintLayout>